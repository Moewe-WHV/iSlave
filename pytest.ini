[pytest]
pythonpath = . src
# Nur der Ordner tests wird gesammelt. Die Prototypen unter
# src/frontend heissen ebenfalls test_*.py, oeffnen beim Import aber
# Tkinter-Fenster und wuerden einen nackten pytest-Lauf blockieren.
testpaths = tests
